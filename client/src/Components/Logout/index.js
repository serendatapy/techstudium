// Import the style for the component
import './index.css';

// Declare the Logout component
function Logout() {
  return (
    <div>
    This is the Logout component
    </div>
  )
}

// Export the Logout component
export default Logout;